name: "ci: c-kernel"

on:
  push:
    branches: [main]
    paths:
      - "TreasuryKernel/**"
      - ".github/workflows/c-kernel.yml"
  pull_request:
    branches: [main]
    paths:
      - "TreasuryKernel/**"
      - ".github/workflows/c-kernel.yml"

jobs:
  build-and-test:
    name: build + test (release + sanitizers)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install libsqlite3-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev

      - name: Build (release)
        run: make -C TreasuryKernel

      - name: Test (release)
        run: make -C TreasuryKernel test

      - name: Build + test (debug with ASan + UBSan)
        run: |
          make -C TreasuryKernel debug
          make -C TreasuryKernel test
